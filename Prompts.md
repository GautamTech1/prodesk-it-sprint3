create the html css layout for dev-detective with a full-width navbar logo search battle mode theme toggle and an empty profile card. set up css variables for dark/light mode.

write vanilla js to fetch user data from the github api using fetch map the avatar name bio and portfolio. include a loading state and handle 404 errors with a user not found banner no axios.

chain a second fetch request to get the user's top 5 latest repositories. create a custom js utility function to format the iso join date into a human-readable format.

implement battle mode for two users. use promiseall to fetch both concurrently use reduce on their repos to calculate total stars and highlight the winner green and loser red.

add the javascript logic to toggle between dark and light mode add a css @keyframes blink/pulse effect to the main subtitle and profile cards on hover.

update the existing javascript and css to add a tab-switching functionality for the 'search' and battle' buttons located on the left and right of the main subtitle github profile search & battle mode.
